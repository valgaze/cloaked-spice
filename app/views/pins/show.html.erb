
<%= image_tag @pin.image.url %>
<p>
  <strong>Description:</strong>
  <%= @pin.description %> 
 
(<%= @pin.description.length %>
 characters)
Last updated: <%= @pin.updated_at.strftime("%l:%M %p %e %B") %>


<% if @pin.user == current_user %>
  <%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>